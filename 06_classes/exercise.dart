// EXERCISE 6 — Classes
// Build a Course class with:
// Properties: name, instructor, students (List<String>), maxCapacity
// Methods:
//   enroll(name) — add if not full, else "Course is full."
//   drop(name)   — remove if exists, else "Student not found."
//   printRoster() — numbered list
//   isFull() → bool
// In main(): create course (max 3), enroll 4 (4th rejected), drop 1, print roster
// BONUS: sessionCount + conductSession() → "Session N completed. X attended."

void main() {
  // write here
}

// write Course class here
